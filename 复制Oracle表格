在项目中，如果需要新建一张新的表格，而且这张表格跟别的表格一模一样，那么需要在表格信息的DDL里把表格的SQL语句复制出来，然后加以修改，以符合新创建表格的
要求；然后再在你需要的Oracle用户里跑这条SQL语句；比如：
CREATE TABLE T_QCEVALUATERESULT(
 	ABSOID CHAR(32) NOT NULL ENABLE, 
	VID CHAR(32) NOT NULL ENABLE, 
	IDOFQCEVALUATERESULT CHAR(32), 
	TITLE VARCHAR2(254), 
	STATUS NUMBER(*,0) DEFAULT 0, 
	CREATIONTIME NUMBER(*,0) DEFAULT 0, 
	LANGUAGE VARCHAR2(254) DEFAULT 'zh-hans', 
	VERSIONNAME VARCHAR2(254), 
	ISLASTVER NUMBER(*,0), 
	WEIGHT FLOAT(126), 
	CREATOR_UID CHAR(32), 
	QINPUTCODE VARCHAR2(20), 
	CREATOR_UAGID CHAR(32), 
	QCEVALUATERESULTCODE VARCHAR2(254), 
	QCEVALUATERESULTITEM VARCHAR2(254), 
	STUDYSITEAGENT_ABSOID CHAR(32), 
	STUDYSITEAGENT_VID CHAR(32), 
	CONTRACTCOUNT VARCHAR2(254), 
	FAILEDCOUNT VARCHAR2(254), 
	BREAKCOUNT VARCHAR2(254), 
	DESCRIPTION VARCHAR2(4000) 
   );
